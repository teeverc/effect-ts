# Versioning Notes

Use this guide when adapting examples across Effect versions.

- Effect 2.3 changed type parameter ordering to `Effect<A, E, R>` (and related types).
- When reading older examples, translate type parameters to the new order.
- Track release notes for breaking changes and align the skill to your project's Effect version.
